{
  "name": "attendance-saas-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix backend & npm run dev --prefix frontend",
    "employee-mobile": "npm start --prefix employee-mobile"
  }
}

