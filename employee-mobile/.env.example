# Copy to .env for local Expo Go testing.
# Use your computer's LAN IP when testing on a physical phone/tablet (not localhost).
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000

# Production APK builds use https://punchpay.in via eas.json (preview/production profiles).
